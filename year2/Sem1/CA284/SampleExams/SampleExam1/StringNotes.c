


%s = string
%c = char
strlen() = length of string
strcpy(destination, source) = from source to destination 
strcat(source, destination) = combines source and destination

strcmp(str1, str2) = returns 0 if true, 1 if not;
>0 if the first non matching in arg1 > arg2
<0 if opposite
